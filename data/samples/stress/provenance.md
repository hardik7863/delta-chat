# stress — two different real drawings (unlabelled)

Rev A = Lift Gas compressor P&ID, Rev B = Export Gas compressor P&ID. Different drawings, so the 'delta' is huge — used only to check the pipeline scales and stays sane on a large diff, not for scored eval.
