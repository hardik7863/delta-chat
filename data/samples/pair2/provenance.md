# pair2 — both revisions SCANNED (OCR adapter)

- **Rev A / Rev B**: a legible synthetic P&ID rendered to PDF, then rasterized to noisy, slightly rotated, image-only PDFs (`src/synth_scan.py`) — no text layer, so both are OCR'd (Tesseract).
- **Ground truth**: 3 edits — size 10"→8", valve 43BL9070 removed, PT-9099 added.
- Dense A3 originals OCR poorly (tiny text); see README → Limitations.
